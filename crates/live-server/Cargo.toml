[package]
name = "live-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "axum server + headless run modes on top of live-core"

[dependencies]
live-core = { path = "../live-core" }
